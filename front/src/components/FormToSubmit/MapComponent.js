import React from 'react'
import { Form } from 'antd'

import { Map, Marker, GoogleApiWrapper } from 'google-maps-react'

const MapComponent = ({ description, index }) => {
	return <h1>MAP!</h1>
}

export default MapComponent
